
## sv_query 项目开发记录

### Phase 2 边类型精细化完成
- CLOCK 边: always_ff 块内创建 clk->q(CLOCK) 边
- RESET 边: always_ff 块内创建 rst->q(RESET) 边
- ENABLE/DATA: 不作为独立边类型，用 condition 属性替代
- 开发纪律：铁律16 - 改动前先评估理想实现

### 模块实例化连接修复
- 修复 inst.decl.name 访问（而非 inst.decl.value）
- EdgeKind.CONNECTION 替代 EdgeKind.DRIVER 用于端口连接
- clean_name 增加 strip() 处理空格
- 添加回退逻辑确保子模块->实例连接边存在

### 测试状态
- 425 passed
- 新增测试: test_reset_edge.py, test_cdc_multiclock.py, test_instance_connection.py


## 11:39 AM - 测试标准更新 & Bug 修复

### 提交
- `961ff95` - docs: 新增测试标准铁律 17-20
- `b511d73` - fix: 多处 bug 修复

### 测试标准更新 (铁律17-20)
- 铁律17: 强断言原则
- 铁律18: 负面测试原则
- 铁律19: RTL来源原则 (真实场景)
- 铁律20: 全面性原则

### 发现的 Bug (b511d73 修复)
1. CLOCK/RESET 边在 27690eb commit 后丢失
2. 多事件敏感信号 @(posedge clk or negedge rst) 提取逻辑缺失
3. PORT_INOUT 支持不完整
4. REG 升级逻辑缺失（被 CLOCK 边驱动的节点应升级为 REG）
5. is_port 标记在升级时丢失

### 当前测试状态
- 424 passed
- 7 failed (来自 test_instance_connection, test_timing_control, test_cdc_multiclock)

### 待完成任务
1. 修复剩余失败的测试
2. A 方案：真实场景测试（需选择项目）
3. B 方案：针对性测试 + 负面测试
