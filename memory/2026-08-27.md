# 2026-08-27 Daily Notes

## 🎯 主线任务: 三份 spec 文档补全 — Phase 1 事实基础落实

**背景**: 用户 13:52 指出三个未完成的任务 — Signal Graph 输入输出 spec、pyslang semantic 使用模式、SV 语法识别准确对应。前期修了几处硬伤但没"做实"，用户 15:11 指示"重新整理一下"，15:13 选定方案 C：**先用 subagent 并行探索 + 落实 golden，再写 spec**。

### Phase 1: 探索（已完成）

派出 3 个 subagent 并行审计（push-based 推送）：

- ✅ **Phase 1A** driver_extractor 30 语法矩阵 — 完成 (7m12s, 71.6k tokens)
- ⚠️ **Phase 1B** SemanticAdapter 76 方法调用方 — 跑 12m+，手工扫描替代完成
- ✅ **Phase 1C** connection_extractor / graph_builder / bit_select_handler — 完成 (1m6s)

### 事实基础汇总（来自 Phase 1）

#### SV 语法真实支持矩阵 (driver_extractor)

**✅ 完整支持（18 种）**: assign / always_ff / always_comb / wire-init (顶层+generate-for) / case 普通 / RHS 位选 / LHS 位选 / {a,b} 拼接赋值 / alias / function / task / 三元（限5层嵌套）/ class / 等

**⚠️ 部分支持（4 种）**: always_latch（按普通 always，无 latch 语义）/ reg/logic array（ElementSelect 字符串化，整数组赋值未生成 driver 边）

**🔶 有条件支持（2 种）**: generate-if/case 内 wire（`get_generate_net_declarations` 只查 GenerateBlockArray，GenerateBlock 单块未处理）

**❌ 不支持（5 种，已独立核验）**:
- `initial` 块 — `driver_extractor.py:3346-3349` 显式 `pass`（原 statement 处理代码注释掉）
- `casez` / `casex` — `_flatten_case` 无 casez/casex 区分分支（2971 把 Case/PatternCase 同等处理）
- `unique case` / `priority case` — 无任何 unique/priority 处理
- **`{W{1'b1}}` 作为 LHS** — `_handle_concat_assign` (1332-1340) 只查 `"Concatenation" in str(raw_lhs.kind)`，Replication 不识别

**🔸 已知缺陷（4 条）**:
- `always_comb` 阻塞/非阻塞无差别处理（`assign_type` 总是 `nonblocking`）
- 嵌套三元限制 5 层解包装（line 2302）
- function 递归可能无限展开（`SubroutineExpander` line 3810）
- `alias a = b` 方向语义与 SV 规范相反（refs[0]=target, refs[1]=source）

#### SemanticAdapter 调用频次矩阵（Phase 1B 完成, 13m53s, 148k tokens）

**真实公开方法 = 53 个**（前版我给的 76 是按 def 总行数算的；subagent 严格按"外部可调用"重数 = 53，差额 23 = 6 嵌套 helper + 17 私有/dunder）。

**🔥 核心方法（调用方 > 5 文件）**: `get_modules`(27×), `get_port_declarations`(13×), `get_module_name`(13×), `extract_port_width`(10×), `get_port_name_and_direction`(10×), `get_assignments`(8×), `get_module_instances`(8×), `get_classes`(7×), `clean_name`(6×)

**高頻（2-5 调用方）**: `get_source_location`(5×), `get_always_blocks`(5×), `get_module_parameters`(3×), `get_genvar_context`(2×), `get_source_text`(2×), `root`(2×), `get_variable_declarations`(2×), `get_data_declarations`(2×), `get_signal_name`(2×), `extract_data_width`(2×)

**低頻 / 单点调用方（1×）**: `get_function_name`/`parser`/`get_modport_info`/`get_generate_instances`/`get_function_declarations`/`get_task_params`/`get_interface_modport_signals`/`get_function_params`/`get_interfaces`/`get_modport_declarations`/`get_instance_connection`/`get_port_names`/`get_port_name`/`get_task_declarations`

**⚠️ UNUSED / 兼容性 stub (0 调用方, 12 个)**:
- `get_visit`, `get_top_level_subroutines`, `get_class_name` — **完全未调用**
- `items`, `trees` — **兼容性 stub**（注释明示"返回空迭代器"）
- `parser`（property）— 仅有1 个 .parser 链调用，但内部是 alias 到 self（兼容性 wrapper）
- `name`(@property, SemanticInstanceDeclWrapper) — 间接访问，未列入统计
- `get_port_name` — 已被 `get_port_name_and_direction` 替代
- `extract_signals_from_expr` — 私有 helper（_extract_signals_from_expr 是真实现）
- `SemanticInstanceWrapper.__init__`, `__str__`, `__repr__`, `_get_module_type`, `get_parent_module`, `_get_parent_module_safe`, `parent` — wrapper 内部方法

> **Spec 修订要点**: 前版 SemanticAdapter 方法总览表把 76 个平铺了。要改为 53 公开方法 + 12 UNUSED 区分。UNUSED 应标 ⚠️ 并备注'兼容性 stub / 后续可删'。

#### 其他 builder 真实情况（Phase 1C）

**connection_extractor.py**:
- inout 端口检测: `"inout" in direction.lower()`（先判断 inout，不会和 input 冲突）
- 未识别 direction fallback 到 PORT_IN（**静默错误**，应该 raise warning）
- generate 嵌套 > 5 退化（`for _ in range(5)` L31/L56，fallback 到 `parent_module`/None）
- 端口位宽 `(0,0)` → fallback `(1,0)` 兜底
- 实例名解析失败 → fallback `str(inst).split("(")[0]`（匿名实例乱码）

**graph_builder.py 11 步管线（spec 已对齐）**:
- `_configure_instance_paths` (target_module 时)
- `_extract_all_nodes`
- `_extract_all_edges`
- `_mark_special_signals`
- `_create_hierarchical_bit_nodes`
- `_collect_struct_members`
- `_expand_struct_assignments`
- `_upgrade_reg_nodes` (must be after bit nodes)
- `_elaborate_wrapper_passthroughs`
- `_filter_by_target` (target_module 时)
- **`_capture_generate_block_map` (V16.11)** — spec 已修

**bit_select_handler.py**:
- 在 unified_tracer 层用（line 467-470）
- graph_builder 不用，自己有 `_create_hierarchical_bit_nodes` — **两套并行实现**，是潜在去重机会

### Phase 2-5 计划

- **Phase 2**: 写完本汇总（即事实基础已落实）
- **Phase 3**: 给每种"✅ 完整"语法建最小 fixture + 测试（golden_mini/ 目录）
- **Phase 4**: 跑全套确认全绿
- **Phase 5**: 反向从 golden 修订三份 spec（删空话、补实测证据链接）