
## 2026-05-14 下午更新

### 新功能：Snapshot 快照管理

**实现文件**：
- `src/trace/core/graph/models.py` - SignalGraph.to_dict() / from_dict() / to_json() / from_json()
- `src/trace/core/snapshot_manager.py` - SnapshotManager CRUD
- `src/cli/commands/snapshot.py` - CLI 子命令
- `sim/tests/integration/test_snapshot.py` - 金标准测试 (7 tests)

**CLI 命令**：
```bash
svq snapshot save <file> -t <tag>   # 保存快照
svq snapshot list                    # 列出快照
svq snapshot show <tag>             # 查看详情
svq snapshot delete <tag>           # 删除
svq snapshot compare <tag1> <tag2> # 对比两个快照
```

### Graph Diff 架构健康度（方案一）

**实现**：
- `compute_stable_core()` - 标识符严格匹配法 O(|V|+|E|)
- `compute_health_score()` - 稳定核心比例
- `compute_coupling_warning()` - 耦合预警（小改动<5% + 高不稳定>30%）
- `diff_with_health()` - 整合所有 phase

### 文档更新

- `docs/architecture/GRAPH_DIFF_DESIGN.md` - 更新 Phase 3 状态（方案一替代）+ Snapshot 实现状态
- `docs/architecture/README.md` - 更新文档列表

### 提交记录

| Commit | 功能 |
|--------|------|
| `b8836c7` | fix: recursively handle nested ScopedNames (p.sub.data) |
| `80c4524` | feat(diff): add 方案一 stable core + health score |
| `f409fdd` | feat(snapshot): add SignalGraph serialization and SnapshotManager |

### 项目状态

- **已完成**：ScopedName 嵌套修复、Graph Diff 健康度、Snapshot 管理
- **测试**：23 个 integration tests 全部通过
- **CLI**：svq trace / diff / snapshot / stats
