
## Visualization Validation Tests (2026-07-10 01:30)

方豆 asked "测试其他的画图功能，是否和代码一致，易于理解"
- Tested 8 viz commands on Ventus L2 Scheduler
- Wrote 13 validation tests in test_ventus_all_viz_validation.py
- All 13 pass

### Findings:
- **arch (depth=1) PNG**: Clear & readable ✅
- **pipeline PNG**: 31k px tall, single-column layout, hard to read ⚠️
- **chain PNG**: Easy to read with 3-color critical path ✅
- **timing**: Text-only summary, clear but no viz ⚠️
- **fanin/fanout**: Empty results (top-level signals, no further use) ✅

### Consistency:
- arch d=1 shows 6 sub-instances (Scheduler has 7 _dut total but arch --depth 1 collapsed directory_test)
- pipeline detects 14 stages, 12 control regs, 33 state regs
- timing: 896 nodes, 59 regs, deepest path depth=2 (D→mem_core→QN)
- CDC: 1 clock domain (Scheduler has only clk input)
- handshake: 0 pairs (Scheduler uses custom *_i/*_o naming, not AXI)

### Inconsistencies:
- Pipeline uses LAYOUT_TB (vertical), should use LR for time-flow
- Pipeline PNG is 31k tall — needs pagination/clustering
- Arch d=1 is missing directory_test.directory_test_dut (collapsed by type?)

## P0 Pipeline fix PUSHED (2026-07-10 07:30)

Commit `6664fbf` PUSHED to main after network recovery.
- PNG 31851px → 4061px (default) / 2003px (--max-control-nodes 0)
- Added --max-comb-per-stage, --max-control-nodes CLI flags
- 23/23 tests pass

## P1 arch --show-all RESOLVED (2026-07-10 07:45)

Root cause: `directory_test.v` was NOT in the filelist fixture, so pyslang
emitted "[ERROR] Scheduler.v:648:3: [UnknownModule]" and arch correctly
excluded the unresolvable instance.

Fix: Added directory_test.v to sim/tests/manual_filelists/ventus_l2_scheduler.f
Now arch depth=1 shows all 7 _dut instances:
  - SourceA, sourceD, sinkA, sinkD, banked_store, Listbuffer, directory_test
Pipeline: 14 stages → 24 stages (more visibility)

No code change needed - just filelist. All 19 tests still pass.

---

# 📋 完整今日 session 总结 (2026-07-10)

## 🎯 主要工作

### 早上: OOM 规避
- 8GB MBA 跑 Ventus GPGPU 失败 (pyslang elaboration 不完整)
- 8GB memory reclaim trick 被你 (方豆) 拒绝 → 改 per-module chunking
- 4 个独立 filelist: l2_distribute / l2_scheduler / sm2cluster / cta
- 4/4 chunk 测试 PASS

### 中午: 多 viz 命令测试
- 方豆 问 "其他画图功能, 是否和代码一致, 易于理解"
- 测试 8 个 viz 命令 (arch/pipeline/chain/trace/cdc/sva/handshake/backpressure)
- 发现 chain 中 6/7 _dut 漏掉 directory_test
  - **根因**: filelist 缺 `directory_test.v`
  - **不是工具 bug, 是 filelist 缺文件**
- 修复后 arch 显示完整 7 个 _dut

### 下午: P0 + P1 + P2 修复

**P0 Pipeline fix (`6664fbf`)**:
- 461 个 control 节点堆成 31k px 高 PNG, 没法看
- 修复: 加 cluster_anomalies + `--max-control-nodes` + `--max-comb-per-stage`
- PNG 31k → 4k (默认) / 2k (隐藏 control)

**P1 filelist fix (`5886210`)**:
- 加 `directory_test.v` → arch 显示完整 7 个 _dut

**P1 timing --dot (`440383d`)**:
- 新功能, 画 critical path DOT
- 关键路径红色高亮 (D→mem_core→QN)

**Anomaly detection 链路**:
- `dd73b9e` chain 检测 X_DRIVER/DANGLING/ORPHAN
- `12ad253` timing + arch 暴露 anomaly
- `a8c5709` **low-confidence warning** (你 16:14 触发)

### Cross-viz 一致性验证 (`090ba71`)
- 6 个测试验证 4 个 viz 命令报告不矛盾
- 互相印证: arch/chain/pipeline/timing 给同一模块不同视角

### Golden testcases (`5a8945b`)
5 个手写 SV 文件, 测试 anomaly 检测:
- `normal` (0 anomalies) ✅
- `x_driver` (1 X_DRIVER = orphan_wire) ✅
- `dangling` (1 DANGLING = unused_reg) ✅
- `combined` (2 X_DRIVER + 1 DANGLING) ✅
- `submodule` (跨模块 port crossing, 0 anomalies) ✅

## 🐛 你发现的关键问题 (2026-07-10 16:14)

"不对吧" - openofdm_tx 报 22-243 anomalies 是误报!

**根因**: pyslang elaboration 内存不足时**静默失败**, 不报错.
- 复杂项目 (openofdm_tx) 在 8GB MBA 上 elaboration 不完整
- 缺 driver edges → input port (clk/reset) 被误标 X_DRIVER
- 缺 consumer edges → 正常信号被误标 DANGLING

**修复 (`a8c5709`)**:
- 加 `_ELABORATION_INCOMPLETE` global flag
- 当 SWAP > 2GB 时, anomaly 报告加 "low confidence" 警告
- 用户能区分: high confidence (确认 bug) vs low confidence (需手动 review)

## 📊 提交链 (5 commits, 全 pushed)

```
a8c5709 fix(anomaly): low-confidence warning when SWAP > 2GB    ← 16:14
12ad253 fix(timing+arch): detect RTL anomalies that truncate      ← 15:23
090ba71 test(cross-viz): verify viz commands consistent facts    ← 12:35
5a8945b feat(chain): golden testcases + full-module scan         ← 10:50
dd73b9e feat(chain): detect X_DRIVER/DANGLING/ORPHAN              ← 09:40
6664fbf fix(visualize): pipeline P0 fix (group control signals)   ← 07:30
5886210 fix(ventus): add directory_test.v to filelist             ← 07:45
440383d feat(timing): add --dot for critical path                  ← 08:22
cce4941 fix(cli): -f flag auto-detects filelist extension
e2aba30 refactor(trace): remove SWAP-reclaim trick (你要求)
2d04a21 test(ventus): viz vs source validation
d92da50 test(ventus): comprehensive viz validation (13 tests)
```

## 📦 测试统计

```
22/22 golden chain tests (5 testcases × ~4 tests + low confidence)
28/28 viz_validation tests
6/6 cross_viz_consistency tests
274+ total CLI tests
```

## ⚠️ 已知限制

1. **大项目 OOM**: openofdm_tx 在 8GB MBA 上 elaboration 不完整
   - 解决: 16GB+ 机器 / 分模块跑 / 加更多内存
2. **chain 在大项目上**: 30+ input × 30+ output 让路径搜索爆内存
3. **pyslang 静默失败**: 这是工具的核心限制, 只能 workaround

## 💡 经验教训 (供未来参考)

1. **永远不要相信"找到 N 个 bug"**, 要看 low confidence 警告
2. **input port 不能是 X_DRIVER** — 如果工具报这个, 一定是 elaboration 不完整
3. **每加新 viz 命令**, 都要在 golden testcase 上验证
4. **DOT 文件太大时要 crop**, 否则 Image 模型看不全
5. **飞书 1 消息最多 1 张图**, 多张要分开发
6. **多 viz 命令的结果要 cross-validate**, 互相印证才可信

